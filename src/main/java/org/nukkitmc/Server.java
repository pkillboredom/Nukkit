package org.nukkitmc;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

/**
 * author: MagicDroidX
 * Nukkit Project
 */
public class Server {

    public static Logger getLogger() {
        return LogManager.getLogger(Server.class.getName());
    }
}
